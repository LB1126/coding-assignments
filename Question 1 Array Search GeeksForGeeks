import java.util.ArrayList;

class Solution {
    public int search(int arr[], int x) {
        ArrayList<Integer> arrL = new ArrayList<>();
        for(int i = 0; i < arr.length; i++){
            arrL.add(arr[i]);
        }
        return arrL.indexOf(x);
    }
}
