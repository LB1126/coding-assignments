class Solution {
    public int countBuildings(int[] height) {
        int greatest = 0;
        int counter = 0;
        for(int i = 0; i < height.length; i++){
            if(greatest < height[i]){
                counter++;
                greatest = height[i];
            }
        }
        return counter;
    }
}
