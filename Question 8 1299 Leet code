class Solution {
public int[] replaceElements(int[] arr) {
    int rightG = -1;
    for(int i = arr.length-1; i >= 0; i--){
            int temp = arr[i];
            arr[i] = rightG;
            if(temp > rightG){
                rightG = temp;
            }
        }
        return arr;
    }
}
